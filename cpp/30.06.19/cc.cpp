#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(int argc, char** argv)
{
    vector <int> a;
    int n;
    for(int i = 0; i < n; i++)
    {
        int temp;
        cin >> temp;
        a.push_back(temp);
    }

    cout << "[]";
    for(int i = 0; i < n; i++)
    {
        cout << "[";
        for(int j = 0; j < i + 1; j++)
        {
            for(int )
        }
    }
}